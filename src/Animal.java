public class Animal extends animalTraits {
    public Animal(int age, int weight, String name, String birthday, String species, String color, String sex,
                  String arrivalDate, String idNumber) {
        super(age, weight, name, birthday, species, color, sex, arrivalDate, idNumber);
    }
}

